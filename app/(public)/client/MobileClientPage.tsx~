'use client';

import React from 'react';
import Image from 'next/image';
import { FaStar, FaShareAlt, FaHeart, FaCheck } from 'react-icons/fa';
import { Calendar } from '@heroui/react';
import { parseDate } from '@internationalized/date';
import ClientContactInfo from '@/components/ClientContactInfo';
import ReviewCard from '@/components/ReviewCard';
import Partner from '@/types/partner';
import {Pagination} from "swiper/modules";
import { Swiper, SwiperSlide } from 'swiper/react';
import 'swiper/css';
import 'swiper/css/pagination';
import exampleImage1 from "@/images/Example1.jpg"
import exampleImage2 from "@/images/Example2.jpg"
import ClientCarousel from "@/components/carousels/ClientCarousel";

export default function MobileClientPage({
                                             partners,
                                         }: {
    partners: Partner[];
}) {
    const services = [
        'Servicii catering',
        'Cazare',
        'Wi-fi',
        'Ospătari',
        'Piscină',
        'Evenimente aer liber',
        'Garderobă',
        'Recepție',
        'Cocktail bar',
    ];

    const images = [exampleImage1, exampleImage2];

    const reviewData = [
        {
            name: 'David Dobrescu',
            date: '7 days ago',
            review: 'Lorem ipsum dolor sit amet, consectetur adipiscing elit.',
            rating: 5,
        },
        {
            name: 'Anca Lungu',
            date: '16 days ago',
            review: 'Rhoncus tempus sed est tempor.',
            rating: 4.5,
        },
    ];

    return (
        <div>
            <ClientCarousel images={images}/>
            <div className="px-4">
                <div className="mt-3 flex justify-between items-center border-b-gray-400">
                    <div>
                        <h1 className="text-xl font-bold">{"NUME"}</h1>
                        <div className="flex items-center text-gray-600 text-sm">
                            <FaStar className="mr-1" /> {"RATING"} - <span className="underline ml-1">22 recenzii</span>
                        </div>
                        <div className="text-sm text-gray-500">{"LOCATION"}</div>
                    </div>
                    <div className="flex space-x-2 text-green-700">
                        <FaShareAlt />
                        <FaHeart />
                    </div>
                </div>

                <button className="mt-4 w-full py-2 bg-green-700 text-white rounded">Sună pentru rezervare</button>

                <div className="mt-6">
                    <h2 className="font-bold text-lg mb-1">Date de contact</h2>
                    <ClientContactInfo />
                </div>

                <div className="mt-6">
                    <h2 className="font-bold text-lg mb-1">Disponibilitate</h2>
                    <Calendar
                        isReadOnly
                        aria-label="Date"
                        defaultValue={parseDate("2025-01-02")}
                    />
                </div>

                <div className="mt-6">
                    <h2 className="font-bold text-lg mb-1">Descriere</h2>
                    <p className="text-sm text-gray-600">
                        Lorem ipsum dolor sit amet, consectetur adipiscing elit. Rhoncus tempus sed est tempor.
                    </p>
                </div>

                <div className="mt-6">
                    <h2 className="font-bold text-lg mb-1">Servicii oferite</h2>
                    <div className="flex flex-wrap gap-2">
                        {services.map((service, index) => (
                            <span
                                key={index}
                                className="flex items-center px-3 py-1 border border-green-700 rounded-full text-sm"
                            >
                                <FaCheck className="mr-2 text-green-700" /> {service}
                            </span>
                        ))}
                    </div>
                </div>

                <div className="mt-6">
                    <h2 className="font-bold text-lg mb-1">Parteneri</h2>
                    <div className="flex gap-2 overflow-x-scroll">
                        {partners.map((partner, index) => (
                            <div key={index} className="min-w-[120px] text-sm text-center">
                                <div className="w-full h-20 bg-gray-200 rounded mb-1" />
                                {partner.name}
                            </div>
                        ))}
                    </div>
                </div>

                <div className="mt-6">
                    <h2 className="font-bold text-lg mb-1">Recenzii</h2>
                    {reviewData.map((review, index) => (
                        <ReviewCard key={index} {...review} />
                    ))}
                </div>

                {/*<div className="mt-6">*/}
                {/*    <h2 className="font-bold text-lg mb-1">Localizare pe hartă</h2>*/}
                {/*    <MapComponent />*/}
                {/*</div>*/}
            </div>
        </div>
    );
}
